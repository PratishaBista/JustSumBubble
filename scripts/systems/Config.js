const config = {
    bubbleCount: 20,
    bubbleColor: 'rgba(255, 255, 255, 0.4)',
    glowIntensity: 40, 
    theme: {
        bg: 'linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 100%)',
        particle: '#ffffff'
    }
};

export default config;
